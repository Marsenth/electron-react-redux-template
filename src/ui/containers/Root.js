import React from 'react';

const Root = () => (
  <div className='app-root'>
    <h1>Su dirección de IP no se encuentra autorizada para utilizar esta aplicación.</h1>
  </div>
);

export default Root;
